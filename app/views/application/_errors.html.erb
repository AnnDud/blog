<% if model.errors.any? %>
	<div>
		<h2>
			<%= pluralize(model.errors.count, 'error') %> prohibited
			this article from being saved:
		</h2>
		<ul>
			<% model.errors.full.messages.each do |msg| %>
			<li><%= msg %></li>
			<% end %>
		</ul>
	</div>
<% end %>